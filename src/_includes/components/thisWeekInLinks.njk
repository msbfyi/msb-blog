{% set thisWeekInLinks = collections.links | first %}

<div class="dispatch">
<span class="st" aria-hidden="true"><svg viewBox="-100 -100 200 200"><use href="#burst"></use></svg></span>
<div class="left">
<span class="k">Current issue</span>
<a href="{{ thisWeekInLinks.url | url }}" class="issue">{{ thisWeekInLinks.data.date | dateFilter }}</a>
<span class="sub">A week of things worth keeping, collected and sent out every Sunday.</span>
</div>
<a href="/this-week-in-links/" class="past btn onblue"><span class="d"></span>See past week's links…</a>
</div>
